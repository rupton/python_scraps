'''
    This is a simple script to convert decimal values to binary values, and binary values 
    to decimal values.
'''

def convert_to_decimal(binary):
    #for number in binary:
     #   print(int(number))
    for i in range(len(binary)):
        print(i)
    return ""

check_val = convert_to_decimal('10110')
print(check_val)